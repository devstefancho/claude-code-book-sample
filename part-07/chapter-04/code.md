# Chapter 04. 코드 모음

## 안전한 설치 준비하기 (설정 백업)

```shell
cp ~/.claude/settings.json ~/.claude/settings.json.backup

cp ~/.claude/settings.local.json ~/.claude/settings.local.json.backup 2>/dev/null
```

## autopilot으로 칸반보드 만들기

```shell
mkdir vibe-kanban && cd vibe-kanban
```

## 개발 서버 실행

```shell
npm run dev
```
